import React from 'react';

interface AdSlotProps {
  type?: 'banner' | 'native';
  className?: string;
  label?: string;
}

/**
 * AdSlot Component
 * Third-party / Adsterra ads removed completely for clean Google AdSense approval review.
 * When AdSense is approved, AdSense Auto Ads or responsive AdSense ad units will be activated.
 */
export const AdSlot: React.FC<AdSlotProps> = () => {
  return null;
};

